<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.academy.academy.admin.mapper.NewUserMapper">

    <insert id="insertNewUser">
        INSERT INTO newuser (
            subject,
            location,
            newname,
            newphone,
            newgender
        ) VALUES (
            #{subject},
            #{location},
            #{newname},
            #{newphone},
            #{newgender}
        )
    </insert>

     <select id="getnewUserList"
              resultType="com.academy.academy.admin.vo.NewUserVo">
      SELECT subject, location, newname, newphone, newgender
       FROM  newuser
    </select>

</mapper>
